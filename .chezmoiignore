# Only add Vivado configurations when we're on a full Linux machine - for
# Mac OS or embedded boards, we don't want these.
{{ if or (ne .chezmoi.os "linux") .embedded }}
.Xilinx
{{ end }}
{{ if ne .chezmoi.os "darwin" }}
.sleep
.wakeup
{{ end }}

